# Threat Model

This document defines what Amnesic Browser is engineered to defend against,
what it explicitly does not attempt to defend against, and — for the things
it does defend against — exactly which mechanism is doing the work and what
that mechanism's limits are. Section 2 reflects corrections made after
verifying the original plan against the pinned dependency
(`electron@43.0.0`, Chromium 150.0.7871.46); see
`docs/adr/0002-electron-43-flag-and-api-corrections.md` for the reasoning
behind those corrections and `research/*.md` for the underlying source
citations.

## 1. Scope

| Threat                                                   | In scope?              | Why                                                                                                                                                                                                          |
| -------------------------------------------------------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Forensic disk analysis after the app closes              | **Yes — primary goal** | This is the core promise: nothing recoverable from disk once the process exits.                                                                                                                              |
| A live attacker with RAM access while the app is running | Partial                | RAM will always contain plaintext page content while browsing — no browser avoids this. We minimize residency time, we do not eliminate it.                                                                  |
| Network-level observer (ISP, Wi-Fi owner)                | Out of scope for v1    | Local footprint and network footprint are separate concerns. Tor/VPN integration is a separate subsystem that may be bolted on later (see CLAUDE.md non-goals — do not implement without explicit approval). |
| Websites fingerprinting/tracking during a session        | Out of scope for v1    | Anti-fingerprinting (canvas, WebGL, font enumeration spoofing) is a distinct, larger problem from local footprint elimination and is explicitly out of scope for v1.                                         |
| OS swap/hibernation files leaking page content           | **Yes**                | This is the most common way "zero footprint" apps quietly fail. See Section 3 and known limitations below — it is a documented gap, not something the app can fully close from inside a userspace process.   |
| Crash reports / telemetry phoning home                   | **Yes**                | Chromium and most app frameworks do this by default, writing to both disk and network.                                                                                                                       |

**v1 priority:** optimize for "nothing recoverable from disk once the
process exits." Anti-fingerprinting and network anonymity are v2/v3
candidates, not v1 blockers.

## 2. Mitigations: what actually protects what

This table supersedes the plan's original mitigation table. Several rows
below differ from what a first pass at the plan proposed, because
verification against Electron 43 found the original mechanism was either a
no-op or an incomplete claim. Where that's the case, the "Notes" column
says so explicitly.

| Artifact / risk                                                                            | Default location or behavior                                                                    | Actual mitigation                                                                                                                                                                                                                                                                                                                                                                | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Cookies / LocalStorage / IndexedDB / Cache API / Service Workers                           | `userData/Partitions/*`                                                                         | In-memory, non-persistent `session.fromPartition('inmemory-session', ...)` with **no** `persist:` prefix                                                                                                                                                                                                                                                                         | The missing `persist:` prefix is what makes the partition memory-only, not the `cache: false` option passed alongside it — that option only toggles the HTTP cache setting for the session and is redundant with the global `disable-http-cache` switch below. Don't rely on `cache: false` alone if the `persist:`-prefix omission is ever refactored away.                                                                                                                                                                                                                                                |
| HTTP disk cache                                                                            | `userData/Cache`                                                                                | `app.commandLine.appendSwitch('disable-http-cache')`                                                                                                                                                                                                                                                                                                                             | Verified current in Electron 43. Only covers the HTTP disk cache — does not cover the GPU shader cache or V8 Code Cache, which are separate stores; this is defense-in-depth on top of redirecting `userData` to tmpfs, not a substitute for it.                                                                                                                                                                                                                                                                                                                                                            |
| GPU shader cache                                                                           | `userData/GPUCache`                                                                             | `app.commandLine.appendSwitch('disable-gpu-shader-disk-cache')`                                                                                                                                                                                                                                                                                                                  | Verified current in Chromium source (`gpu/config/gpu_switches.cc`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `Local State` file (Chromium global prefs)                                                 | `userData/Local State`                                                                          | Redirect the entire `userData` path to a RAM-backed location (Section 3)                                                                                                                                                                                                                                                                                                         | Primary control; the individual per-subsystem switches above are belt-and-suspenders on top of this, not the main mechanism.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Crash dumps (Crashpad)                                                                     | `userData/Crashpad`                                                                             | **Never call `crashReporter.start()`** anywhere in the codebase, enforced by a CI/lint grep for `crashReporter.start(`                                                                                                                                                                                                                                                           | Electron's `crashReporter` module is opt-in — nothing auto-starts it in Electron 43. The command-line switches `disable-crash-reporter` and `disable-breakpad` do **not** reliably do anything on current Chromium/Electron (neither is confirmed as a live, documented control) and must not be cited as the guarantee. They're kept in `main.js` as harmless defense-in-depth only. See ADR 0002.                                                                                                                                                                                                         |
| Referrer leakage                                                                           | `Referer` request header sent per Chromium's default policy (`strict-origin-when-cross-origin`) | `session.webRequest.onBeforeSendHeaders` to strip/rewrite `Referer`, and/or `session.webRequest.onHeadersReceived` to inject `Referrer-Policy: no-referrer`                                                                                                                                                                                                                      | The plan's original `app.commandLine.appendSwitch('no-referrers')` is a dead, pre-2015 switch with no effect in current Chromium — it is not used. This mitigation requires actual interception code and tests, not a flag. See ADR 0002.                                                                                                                                                                                                                                                                                                                                                                   |
| WebRTC IP leak (real IP visible behind a VPN/proxy)                                        | WebRTC ICE candidates can expose local/public IP regardless of proxy settings                   | Three layers together: (1) `webContents.setWebRTCIPHandlingPolicy('disable_non_proxied_udp')` applied to every window/tab's `webContents`; (2) a preload script deleting `window.RTCPeerConnection`, `window.RTCDataChannel`, and `navigator.mediaDevices.getUserMedia` before page scripts run; (3) `session.setPermissionRequestHandler` denying `'media'` permission requests | There is no single Chromium switch that disables WebRTC outright (`--disable-webrtc` does not exist). No single layer here is a complete guarantee on its own — a `webContents`-level policy plus a JS-level deletion cover different bypass paths (e.g., an RTCPeerConnection created before the preload script runs would only be caught by layer 1). `setWebRTCIPHandlingPolicy` is a `webContents` method, not a `session` method, so it must be reapplied to every new tab/`BrowserView`, typically via an `app.on('web-contents-created', ...)` hook. See ADR 0002.                                   |
| Component updater / Widevine CDM                                                           | `userData/WidevineCdm`, etc.                                                                    | `app.commandLine.appendSwitch('disable-component-update')`                                                                                                                                                                                                                                                                                                                       | Stock `electron@43.0.0` does not ship Widevine at all, so this is moot for DRM specifically unless a Widevine-enabled fork is adopted later (out of scope). The switch also disables the entire component updater, including the Certificate Transparency component — an accepted side effect, not a bug.                                                                                                                                                                                                                                                                                                   |
| Background phone-home (background networking, translate, optimization hints, media router) | Various background Chromium subsystems                                                          | `app.commandLine.appendSwitch('disable-background-networking')`; `app.commandLine.appendSwitch('disable-features', 'Translate,OptimizationHints,MediaRouter')`                                                                                                                                                                                                                   | `disable-background-networking` was designed by Chromium for benchmarking noise reduction, not as a formal privacy control — it is not a guaranteed exhaustive kill-switch for all background network activity. Treat it as one layer among several, not the whole mitigation.                                                                                                                                                                                                                                                                                                                              |
| Safe Browsing hash lookups                                                                 | Compiled into Chromium; would call Google's backend if reachable                                | `app.commandLine.appendSwitch('disable-features', 'SafeBrowsing')` as defense-in-depth; **never set `process.env.GOOGLE_API_KEY`**                                                                                                                                                                                                                                               | Safe Browsing's code path is compiled into Electron 43 by default (Electron dropped the ability to compile it out entirely as of Electron 31), but its network calls require a `GOOGLE_API_KEY` that stock Electron does not ship. Without the app ever setting that variable, Safe Browsing is functionally inert — it is not phoning home by default. The `disable-features` switch is defense-in-depth against a future regression (e.g., someone adding a `GOOGLE_API_KEY` for an unrelated feature), not the thing closing an active leak. Do not add `GOOGLE_API_KEY` to this project for any reason. |
| Spellchecker dictionaries                                                                  | `userData/Dictionaries`                                                                         | `session.setSpellCheckerEnabled(false)`                                                                                                                                                                                                                                                                                                                                          | Verified current, no change from the original plan.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Autofill / saved passwords                                                                 | `userData/Web Data`, `Login Data`                                                               | Never implement autofill or password-manager features (see CLAUDE.md non-goals)                                                                                                                                                                                                                                                                                                  | N/A if the feature is never built — the strongest form of mitigation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `userData` redirected to RAM (Linux)                                                       | `app.getPath('userData')` defaults to `~/.config/<app>`                                         | `app.setPath('userData', '/dev/shm/amnesia-browser-<pid>')`, called before `app.ready` and before any `session.fromPartition()` call                                                                                                                                                                                                                                             | **The target directory must be created first** (`fs.mkdirSync(path, { recursive: true })`) — `app.setPath` throws `Error: ... does not exist` on Electron 43 if the directory isn't already there. macOS and Windows need different approaches (no default writable tmpfs); see the per-OS caveats in the known limitations below.                                                                                                                                                                                                                                                                          |
| Windows Jump List / recent items                                                           | Windows tracks recently opened files/apps for the taskbar                                       | `app.setJumpList([])` (an **empty array**) to clear Jump List content, combined with never calling `app.addRecentDocument()` anywhere in the codebase, enforced by CI grep                                                                                                                                                                                                       | `app.setJumpList(null)` does **not** disable Jump List tracking — passing `null` _restores_ Windows' default automatic Jump List (recent/frequent items). Only an empty array clears the custom Jump List content, and `addRecentDocument` is the API that actually feeds the tracking regardless of the `setJumpList` argument.                                                                                                                                                                                                                                                                            |
| `recently-used.xbel` (Linux GTK recent-files tracking)                                     | GTK's native file chooser tracks recently opened files independently of Electron                | No Electron-level mitigation exists                                                                                                                                                                                                                                                                                                                                              | `dialog`'s `dontAddToRecent` option is **Windows-only** and does nothing on Linux — it does not suppress GTK's `recently-used.xbel` entry. The realistic mitigations are avoiding native file open/save dialogs entirely (already true for v1, since downloads/file-save are non-goals) or, if a dialog becomes unavoidable, post-processing the xbel file after the dialog closes — which is itself a real disk write requiring its own ADR per CLAUDE.md's non-tmpfs-write rule.                                                                                                                          |
| Session/history restore                                                                    | Electron does not implement "restore previous session" by default                               | Never call session-restore APIs                                                                                                                                                                                                                                                                                                                                                  | N/A if the feature is never built.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

## 3. Memory hygiene (best-effort, not perfect)

RAM is not zeroed when a process exits — the OS marks pages free but does
not scrub them until reallocated, and tooling exists to recover
recently-freed memory. Chromium's own allocator is not something this
project controls, so perfect memory scrubbing is not achievable. The
reduction implemented:

- On `window-all-closed`, explicitly `await` `session.clearStorageData()`,
  `session.clearCache()`, and `session.clearAuthCache()` before calling
  `app.exit(0)` — this drops Chromium's own references and lets the
  allocator reclaim faster. `app.exit()` is used instead of `app.quit()`
  because `app.exit()` is immediate and skips `will-quit`, so any cleanup
  must be awaited inline before the call, not hooked to a lifecycle event
  that `app.exit()` would skip.
- No tray icon, no background mode: `window-all-closed` always quits on
  every platform (macOS's default of staying alive on window close is
  explicitly overridden).

**Honest framing:** "no recoverable trace on disk after exit" is a
supportable claim under this design. "No trace in RAM while running, or
immunity to a live memory-dump attack" is not achievable by any browser,
Amnesic Browser included, and this document does not claim otherwise.

## 4. Known limitations

Being upfront about what this project cannot fix is part of its
credibility, not a weakness to hide:

1. If the OS swaps or hibernates while the app is open, plaintext session
   data can land on disk outside the app's control. There is no
   in-process mitigation for this. Users who need this guarantee should
   disable hibernation and use encrypted swap (or no swap).
2. Live RAM forensics while the app is running can recover data — true of
   every browser, not a defect specific to this one.
3. Network-level observers (ISP, router owner) can still see traffic
   unless Tor/VPN is added — that is a separate subsystem, explicitly out
   of scope for v1, and not covered by "local footprint elimination."
4. Websites can still fingerprint/track a visitor during the session —
   local footprint elimination is not the same problem as
   anti-fingerprinting, and this project does not attempt the latter in
   v1.
5. `userData`-to-RAM redirection is not uniform across platforms: trivial
   on Linux (`/dev/shm` is tmpfs by default), materially weaker on macOS
   (no default writable tmpfs; requires shelling out to create a RAM disk
   or falling back to per-subsystem disabling plus a securely-deleted
   temp dir) and on Windows (no native tmpfs; realistic v1 approach is
   per-subsystem disabling as the primary defense, with anything that
   must be written going to `os.tmpdir()` and being securely overwritten,
   not just unlinked, before deletion — a plain delete leaves the
   underlying data blocks recoverable on disks without TRIM/encryption).
6. The crash-reporter guarantee (Section 2) depends on an enforced CI
   lint rule, not a runtime flag. If that rule is ever removed, weakened,
   or bypassed in a future PR, the guarantee silently disappears with no
   other signal.
7. The WebRTC mitigation (Section 2) is layered and each layer has a gap
   the others cover; it is not a single verifiable kill-switch, because
   Chromium does not expose one.
8. Safe Browsing's code path is present in the binary even though it is
   functionally inert by default (see Section 2) — a future change that
   adds a `GOOGLE_API_KEY` for any reason (even unrelated to Safe
   Browsing) would silently reactivate it. This is a standing constraint
   on any future dependency or feature work, not just a one-time check.
