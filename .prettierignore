# Build output and generated artifacts
node_modules
out
dist
dist-electron
coverage
playwright-report
test-results
package-lock.json

# Verbatim filter-list snapshots and scriptlet resources (ADR 0013): these are
# byte-for-byte upstream data with SHA-256 recorded in ATTRIBUTION.md. Prettier
# must never rewrite them — reformatting would corrupt the data and invalidate
# the recorded checksums. ATTRIBUTION.md itself is generated by
# scripts/update-blocklists.mjs.
resources/adblock

# Hermetic e2e fixtures are generated at test time
tests/fixtures/.generated-*

# Tooling/config directories outside the app source
.claude
.mcp.json
